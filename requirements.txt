# Pinned to exact versions tested together. The Dockerfile builds with
# --no-cache, so an unpinned `>=` here can silently pull in a newer release
# with breaking behavior (this is how we hit sqlmodel 0.0.47's stricter
# datetime handling in production while dev was still on 0.0.42). Bump
# deliberately, re-run the test suite, and smoke-test before pushing.
fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlmodel==0.0.47
jinja2==3.1.6
python-multipart==0.0.32
itsdangerous==2.2.0
